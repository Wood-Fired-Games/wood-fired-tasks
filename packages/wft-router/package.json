{
  "name": "@wood-fired-games/wft-router",
  "version": "2.0.0",
  "description": "Event-router daemon that subscribes to the wood-fired-tasks SSE event stream and dispatches matched task events to vendor-neutral handlers (create_task_in_project, webhook_post, shell_exec, agent_session_dispatch). See docs/event-router-design.md for the design-of-record.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "wft-router": "./dist/bin/wft-router.js"
  },
  "files": [
    "dist/",
    "README.md",
    "triggers.example.yaml"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p ."
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "pino": "^10.3.1",
    "yaml": "^2.6.1",
    "zod": "^4.3.6"
  }
}
