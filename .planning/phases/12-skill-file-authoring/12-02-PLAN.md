---
phase: 12-skill-file-authoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tasks/show-task.md
  - skills/tasks/search.md
  - skills/tasks/pick-up.md
autonomous: true

must_haves:
  truths:
    - "User can view full task details via /tasks:show-task with task ID"
    - "User can search tasks by keyword via /tasks:search"
    - "User can pick up a task via /tasks:pick-up (assigns to self, sets in_progress)"
  artifacts:
    - path: "skills/tasks/show-task.md"
      provides: "Task detail view skill with comments and dependencies"
      contains: "wood-fired-bugs:get_task"
    - path: "skills/tasks/search.md"
      provides: "Task search by keyword skill"
      contains: "wood-fired-bugs:list_tasks"
    - path: "skills/tasks/pick-up.md"
      provides: "Task pickup skill with assign + status transition"
      contains: "wood-fired-bugs:update_task"
  key_links:
    - from: "skills/tasks/show-task.md"
      to: "wood-fired-bugs MCP server"
      via: "wood-fired-bugs:get_task, get_comments, get_dependencies tool invocations"
      pattern: "wood-fired-bugs:(get_task|get_comments|get_dependencies)"
    - from: "skills/tasks/search.md"
      to: "wood-fired-bugs MCP server"
      via: "wood-fired-bugs:list_tasks tool invocation with search filter"
      pattern: "wood-fired-bugs:list_tasks"
    - from: "skills/tasks/pick-up.md"
      to: "wood-fired-bugs MCP server"
      via: "wood-fired-bugs:update_task tool invocation"
      pattern: "wood-fired-bugs:update_task"
---

<objective>
Create three Claude Code skill files for task retrieval and action workflows: show-task, search, and pick-up.

Purpose: Enable users to view task details, search for tasks by keyword, and claim tasks for themselves through `/tasks:*` slash commands.
Output: Three markdown skill files in `skills/tasks/` directory.
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-skill-file-authoring/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create show-task skill file</name>
  <files>skills/tasks/show-task.md</files>
  <action>
Create directory `skills/tasks/` (mkdir -p) then create `skills/tasks/show-task.md`.

YAML frontmatter:
- name: show-task
- description: "Shows full details of a task including comments and dependencies. Use when user wants to see task details, inspect a task, or review a specific task."
- argument-hint: [task-id]
- disable-model-invocation: false

Markdown body (multi-tool workflow):
1. Validate task ID from $ARGUMENTS[0] (required, must be positive integer).
2. Call `wood-fired-bugs:get_task` with id parameter to get task details.
3. Call `wood-fired-bugs:get_comments` with task_id to get comments.
4. Call `wood-fired-bugs:get_dependencies` with task_id to get dependency relationships.
5. Format output showing:
   - Header: Task ID, title, status (with visual indicator), priority
   - Details: description, assignee, project, created_by, created_at, updated_at
   - Estimated time and due date (if set)
   - Tags (if any)
   - Dependencies section: tasks this blocks, tasks blocking this
   - Comments section: chronological list with author and timestamp
6. If task not found, display error message.

Keep under 80 lines total. Use imperative form.
  </action>
  <verify>File exists at skills/tasks/show-task.md. Has valid frontmatter with argument-hint. Body references `wood-fired-bugs:get_task`, `wood-fired-bugs:get_comments`, and `wood-fired-bugs:get_dependencies` (all fully qualified). Under 100 lines.</verify>
  <done>skills/tasks/show-task.md exists with correct frontmatter, references get_task/get_comments/get_dependencies tools, and formats a comprehensive task detail view.</done>
</task>

<task type="auto">
  <name>Task 2: Create search skill file</name>
  <files>skills/tasks/search.md</files>
  <action>
Create `skills/tasks/search.md`.

YAML frontmatter:
- name: search
- description: "Searches tasks by keyword across titles and descriptions. Use when user wants to find tasks, look up work items, or search for specific topics."
- argument-hint: [keyword]
- disable-model-invocation: false

Markdown body (single-tool workflow):
1. Extract search keyword from $ARGUMENTS (required, at least 1 character).
2. Call `wood-fired-bugs:list_tasks` with filter: { search: keyword }.
3. Format results as a list showing for each matching task:
   - ID, title, status, priority, assignee (if set)
4. Show total count of matching tasks.
5. If no results found, suggest broadening the search term.

Keep under 50 lines total. Use imperative form.
  </action>
  <verify>File exists at skills/tasks/search.md. Has valid frontmatter. Body references `wood-fired-bugs:list_tasks` with search filter (fully qualified). Under 100 lines.</verify>
  <done>skills/tasks/search.md exists with correct frontmatter, references list_tasks with search parameter, formats results as a list with count.</done>
</task>

<task type="auto">
  <name>Task 3: Create pick-up skill file</name>
  <files>skills/tasks/pick-up.md</files>
  <action>
Create `skills/tasks/pick-up.md`.

YAML frontmatter:
- name: pick-up
- description: "Assigns a task to the current user and sets status to in_progress. Use when user wants to start working on a task, pick up a task, or claim a task."
- argument-hint: [task-id]
- disable-model-invocation: false

Markdown body (multi-tool workflow):
1. Validate task ID from $ARGUMENTS[0] (required, positive integer).
2. Get current user identity (use 'user' as placeholder).
3. Call `wood-fired-bugs:update_task` with:
   - id: task ID
   - updates: { assignee: 'user', status: 'in_progress' }
4. Confirm assignment:
   - Display: "Task <id> assigned to <user> and set to 'in_progress'"
   - Show task title and priority
   - Mention estimated time if present

Include valid status transitions reference:
- open -> in_progress (normal pickup)
- blocked -> in_progress (unblocking and starting)

Note: If task is already in_progress or done, inform user of current status before updating.

Keep under 60 lines total. Use imperative form.
  </action>
  <verify>File exists at skills/tasks/pick-up.md. Has valid frontmatter with argument-hint. Body references `wood-fired-bugs:update_task` (fully qualified). Mentions status transition to in_progress and assignee update. Under 100 lines.</verify>
  <done>skills/tasks/pick-up.md exists with correct frontmatter, references update_task with assignee and status updates, documents valid status transitions.</done>
</task>

</tasks>

<verification>
1. All three files exist in skills/tasks/ directory
2. Each file has valid YAML frontmatter with required fields (name, description)
3. All MCP tool references use fully qualified names (wood-fired-bugs:tool_name)
4. No skill file exceeds 100 lines
5. Descriptions are third-person and include trigger phrases
6. show-task uses three tools (get_task, get_comments, get_dependencies)
7. pick-up updates both assignee and status in single update_task call
</verification>

<success_criteria>
- skills/tasks/show-task.md displays full task details with comments and dependencies
- skills/tasks/search.md searches tasks by keyword via list_tasks search filter
- skills/tasks/pick-up.md assigns task to user and transitions to in_progress
- All three files have valid YAML frontmatter and follow skill authoring best practices
</success_criteria>

<output>
After completion, create `.planning/phases/12-skill-file-authoring/12-02-SUMMARY.md`
</output>
