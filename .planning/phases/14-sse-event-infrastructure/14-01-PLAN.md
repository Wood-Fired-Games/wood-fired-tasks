---
phase: 14-sse-event-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/events/event-bus.ts
  - src/events/__tests__/event-bus.test.ts
  - src/events/types.ts
autonomous: true

must_haves:
  truths:
    - "Services publish typed events after successful operations"
    - "Multiple consumers receive same event independently"
    - "Consumers can unsubscribe from events"
  artifacts:
    - path: "src/events/event-bus.ts"
      provides: "EventBus class with TypeScript generics"
      exports: ["EventBus"]
    - path: "src/events/types.ts"
      provides: "Event type definitions for task lifecycle"
      exports: ["TaskEventType", "TaskEvent", "ProjectEvent", "EventPayload"]
    - path: "src/events/__tests__/event-bus.test.ts"
      provides: "Comprehensive EventBus tests"
      min_lines: 50
  key_links:
    - from: "src/events/event-bus.ts"
      to: "Node.js EventEmitter"
      via: "extends EventEmitter"
      pattern: "extends EventEmitter"
    - from: "src/events/event-bus.ts"
      to: "src/events/types.ts"
      via: "imports event types"
      pattern: "import.*from.*types"
---

<objective>
Implement type-safe EventBus using native Node.js EventEmitter with comprehensive test coverage using TDD methodology.

Purpose: Foundation for real-time event streaming - EventBus decouples event producers (services) from consumers (SSE manager, workflows). Using native EventEmitter with TypeScript generics provides zero-dependency pub/sub with compile-time type safety.

Output: Working EventBus class with passing tests, event type definitions for task lifecycle events.
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/task.ts
@src/services/errors.ts
</context>

<tasks>

<task type="auto">
  <name>Create EventBus with TDD - RED phase</name>
  <files>src/events/__tests__/event-bus.test.ts, src/events/types.ts</files>
  <behavior>
    EventBus provides type-safe pub/sub for domain events:

    Test cases:
    1. emit('task.created', payload) → all subscribers receive payload
    2. subscribe(handler) → handler called with correct typed payload
    3. unsubscribe() → handler no longer receives events
    4. emit with no subscribers → no errors thrown
    5. multiple subscribers → all receive same event
    6. subscriber throws error → other subscribers still execute

    Event types to support in Phase 14:
    - task.created, task.updated, task.deleted
    - task.status_changed
    - project.created, project.updated, project.deleted

    NOTE: task.claimed type defined but emission deferred to Phase 15 (atomic claim endpoint).

    Payload structure:
    - eventType: string (e.g., "task.created")
    - timestamp: ISO 8601 string
    - data: entity snapshot (full Task or Project object)
    - metadata: { source: 'user' | 'workflow', actor?: string }
  </behavior>
  <action>
Create failing tests in src/events/__tests__/event-bus.test.ts:

1. Create src/events/types.ts with type definitions:
   - TaskEventType union ('task.created' | 'task.updated' | 'task.deleted' | 'task.status_changed' | 'task.claimed')
   - ProjectEventType union
   - EventPayload<T> generic interface { eventType, timestamp, data: T, metadata }
   - TaskEvent = EventPayload<Task & { tags: string[] }>
   - ProjectEvent = EventPayload<Project>
   - Document: task.claimed type defined but emission happens in Phase 15

2. Write tests importing non-existent EventBus class
3. Test all cases listed in behavior section
4. Run tests - must FAIL (no implementation yet)
5. Commit: `test(14-01): add failing tests for EventBus`

Research finding: EventEmitter has typed generics support since @types/node July 2024.
  </action>
  <verify>
Run tests (should fail):
```bash
npm test src/events/__tests__/event-bus.test.ts
```

Expected: Tests fail with "Cannot find module 'src/events/event-bus.ts'"
  </verify>
  <done>
Failing tests written for EventBus functionality, types.ts created with event type definitions.
  </done>
</task>

<task type="auto">
  <name>Create EventBus with TDD - GREEN phase</name>
  <files>src/events/event-bus.ts</files>
  <implementation>
Create src/events/event-bus.ts to make tests pass:

1. Import types from src/events/types.ts
2. Create generic class EventBus<Events extends Record<string, unknown>> extending EventEmitter
3. Implement emit<K extends keyof Events>(event: K, payload: Events[K]): void
4. Implement subscribe<K extends keyof Events>(event: K, handler: (payload: Events[K]) => void): () => void
   - Returns cleanup function for unsubscribe
   - Use super.on internally with type safety wrapper
5. Wrap handler calls in try/catch to prevent subscriber errors from crashing EventBus (Pitfall prevention)
6. Export singleton instance:
   ```typescript
   type AppEvents = {
     'task.created': TaskEvent,
     'task.updated': TaskEvent,
     'task.deleted': TaskEvent,
     'task.status_changed': TaskEvent,
     'task.claimed': TaskEvent,  // Defined but not emitted in Phase 14
     'project.created': ProjectEvent,
     'project.updated': ProjectEvent,
     'project.deleted': ProjectEvent
   };
   export const eventBus = new EventBus<AppEvents>();
   ```

Run tests until all pass, then commit: `feat(14-01): implement EventBus with TypeScript generics`
  </implementation>
  <action>
Implement EventBus class in src/events/event-bus.ts following the implementation spec above. Write minimal code to pass tests - no over-engineering.
  </action>
  <verify>
Run tests (should pass):
```bash
npm test src/events/__tests__/event-bus.test.ts
```

Expected: All tests pass, zero TypeScript errors
  </verify>
  <done>
EventBus implementation complete with all tests passing. Singleton instance exported for application use.
  </done>
</task>

<task type="auto">
  <name>Create EventBus with TDD - REFACTOR phase (if needed)</name>
  <files>src/events/event-bus.ts</files>
  <action>
Review EventBus implementation for code quality:
- Extract duplicated logic
- Add JSDoc comments for public methods
- Ensure error handling is robust
- Verify type safety throughout

If refactoring performed, run tests again and commit: `refactor(14-01): clean up EventBus implementation`

If no refactoring needed (code already clean), skip this task and commit message.
  </action>
  <verify>
Run tests after refactoring:
```bash
npm test src/events/__tests__/event-bus.test.ts
```

Check TypeScript compilation:
```bash
npm run build
```

Expected: All tests still pass, zero TypeScript errors
  </verify>
  <done>
EventBus code is clean, documented, and fully tested. TDD cycle complete.
  </done>
</task>

</tasks>

<verification>
Run tests:
```bash
npm test src/events/__tests__/event-bus.test.ts
```

All tests must pass before proceeding to service integration.
</verification>

<success_criteria>
- [ ] RED: Tests written and failing (no EventBus implementation)
- [ ] GREEN: EventBus implementation makes all tests pass
- [ ] REFACTOR: Code clean, typed, documented (if needed)
- [ ] 2-3 atomic commits (test, feat, refactor if needed)
- [ ] Zero TypeScript errors in src/events/
- [ ] Test coverage includes error handling and edge cases
- [ ] task.claimed type defined in types.ts with note about Phase 15 emission
</success_criteria>

<output>
After completion, create `.planning/phases/14-sse-event-infrastructure/14-01-SUMMARY.md`
</output>
