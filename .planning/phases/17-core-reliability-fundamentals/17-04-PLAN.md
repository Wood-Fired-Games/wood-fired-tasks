---
phase: 17-core-reliability-fundamentals
plan: 04
type: tdd
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - src/config/__tests__/env.test.ts
  - src/utils/__tests__/exit-codes.test.ts
  - src/api/__tests__/health.test.ts
autonomous: true
requirements:
  - RELI-01
  - RELI-02
  - RELI-06
  - RELI-08
must_haves:
  truths:
    - Configuration validation tests verify fail-fast behavior
    - Health endpoint tests verify component status reporting
    - Exit codes tests verify sysexits.h values
    - Logging tests verify redaction in production mode
  artifacts:
    - path: src/config/__tests__/env.test.ts
      provides: Tests for configuration validation with Zod
      exports: []
    - path: src/utils/__tests__/exit-codes.test.ts
      provides: Tests for exit code constants
      exports: []
    - path: src/api/__tests__/health.test.ts
      provides: Tests for enhanced health endpoint
      exports: []
  key_links:
    - from: src/config/__tests__/env.test.ts
      to: src/config/env.ts
      via: import { configSchema }
      pattern: configSchema\.safeParse
---

<objective>
Create comprehensive tests for configuration validation, health endpoint, exit codes, and logging behavior.

Purpose: Verify reliability features work correctly through automated tests, enabling regression detection and safe future changes.
Output: Test suites for config validation, exit codes, health endpoint
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/wood-fired-bugs/vitest.config.ts
</execution_context>

<context>
@/home/stuart/wood-fired-bugs/.planning/phases/17-core-reliability-fundamentals/17-RESEARCH.md
@/home/stuart/wood-fired-bugs/src/api/routes/health.ts
@/home/stuart/wood-fired-bugs/src/api/__tests__/health.test.ts
</context>

<tasks>

<task type="auto">
  <name>RED: Add failing tests for configuration validation</name>
  <files>src/config/__tests__/env.test.ts</files>
  <action>
    Create RED tests (failing) for src/config/env.ts:

    1. Test valid configuration parsing:
       - All env vars present and valid
       - Optional vars use defaults
       - Transform functions work (PORT string to number)

    2. Test invalid configuration handling:
       - Missing API_KEYS causes validation error
       - Invalid NODE_ENV value rejected
       - Empty string for required field rejected
       - Transform failures handled (PORT=abc)

    3. Test default values:
       - PORT defaults to 3000
       - HOST defaults to '0.0.0.0'
       - LOG_LEVEL defaults to 'info'
       - NODE_ENV defaults to 'development'
       - Timeouts have sensible defaults

    4. Test type transforms:
       - PORT string becomes number
       - CONNECTION_TIMEOUT string becomes number
       - Boolean transforms if any

    Use vi.mock to isolate tests from actual process.env.
    Each test should fail initially (RED phase).

    Structure:
    describe('config validation', () => {
      beforeEach(() => { vi.resetModules(); });

      it('should parse valid configuration', ...);
      it('should fail on missing API_KEYS', ...);
      it('should use defaults for optional values', ...);
    });
  </action>
  <verify>
    - Tests fail when run (RED phase)
    - Tests cover all config schema fields
    - Tests verify error messages are clear
  </verify>
  <done>
    RED: Failing tests created for config validation covering valid configs, invalid configs, defaults, and transforms
  </done>
</task>

<task type="auto">
  <name>GREEN: Implement config validation to pass tests</name>
  <files>src/config/env.ts</files>
  <action>
    Implement src/config/env.ts to make tests pass (GREEN phase):

    1. Create Zod schema matching test expectations:
       - NODE_ENV: z.enum with default
       - PORT: z.string().min(1).transform(Number).default('3000')
       - HOST: z.string().default('0.0.0.0')
       - LOG_LEVEL: z.enum with default
       - API_KEYS: z.string().min(1, 'message')
       - DATABASE_PATH: z.string().default()
       - Timeouts: all with transforms and defaults

    2. Implement safeParse with clear error messages:
       - Iterate through error.issues
       - Format: "field: message"
       - Exit with code 78 (EX_CONFIG)

    3. Export typed config object

    4. Export ExitCodes constant with sysexits.h values

    Run tests and verify they pass.
    Commit: "feat(17-01): implement configuration validation with Zod"
  </action>
  <verify>
    - All config tests pass (GREEN phase)
    - TypeScript compiles
    - Error messages are clear and helpful
  </verify>
  <done>
    GREEN: Config validation implementation passes all tests
  </done>
</task>

<task type="auto">
  <name>RED: Add failing tests for exit codes</name>
  <files>src/utils/__tests__/exit-codes.test.ts</files>
  <action>
    Create RED tests for exit codes module:

    1. Test ExitCodes constants match sysexits.h:
       - EX_OK = 0
       - EX_USAGE = 64
       - EX_DATAERR = 65
       - EX_NOINPUT = 66
       - EX_UNAVAILABLE = 69
       - EX_SOFTWARE = 70
       - EX_OSERR = 71
       - EX_CANTCREAT = 73
       - EX_IOERR = 74
       - EX_TEMPFAIL = 75
       - EX_PROTOCOL = 76
       - EX_NOPERM = 77
       - EX_CONFIG = 78

    2. Test CliExitCodes constants:
       - SUCCESS = 0
       - GENERAL_ERROR = 1
       - USAGE_ERROR = 2
       - CONFIG_ERROR = 78

    3. Test that codes are readonly (const assertion)

    Each test should fail initially.

    Structure:
    describe('ExitCodes', () => {
      it('should define EX_OK as 0', ...);
      it('should define EX_CONFIG as 78', ...);
      // etc.
    });
  </action>
  <verify>
    - Tests fail when run (RED phase)
    - Tests cover all exit code constants
  </verify>
  <done>
    RED: Failing tests created for exit codes matching sysexits.h values
  </done>
</task>

<task type="auto">
  <name>GREEN: Implement exit codes to pass tests</name>
  <files>src/utils/exit-codes.ts</files>
  <action>
    Implement exit codes module to make tests pass:

    1. Create ExitCodes object with all sysexits.h values
    2. Create CliExitCodes with simplified codes
    3. Use 'as const' for type safety
    4. Add JSDoc comments

    Run tests and verify they pass.
    Commit: "feat(17-03): add sysexits.h standard exit codes"
  </action>
  <verify>
    - All exit code tests pass
    - Values match sysexits.h standard
  </verify>
  <done>
    GREEN: Exit codes implementation passes all tests
  </done>
</task>

<task type="auto">
  <name>RED: Add failing tests for enhanced health endpoint</name>
  <files>src/api/__tests__/health.test.ts</files>
  <action>
    Update/create tests for enhanced health endpoint:

    1. Test healthy response:
       - Returns 200 status
       - status field is 'healthy'
       - includes timestamp
       - includes version
       - checks.database is 'ok'
       - checks.eventBus is 'ok' or 'degraded'
       - checks.sseManager is 'ok' or 'degraded'

    2. Test unhealthy response (database down):
       - Returns 503 status
       - status field is 'unhealthy'
       - checks.database is 'failed'

    3. Test response schema validation:
       - All required fields present
       - Types are correct (strings, objects)

    4. Test component stats (if included):
       - eventBus.stats.listenerCount is number
       - sseManager.stats.clientCount is number

    Use existing test patterns from src/api/__tests__/health.test.ts.
    Mock database, eventBus, and sseManager for controlled testing.

    Tests should fail initially (health endpoint not yet enhanced).
  </action>
  <verify>
    - Health tests fail (RED phase)
    - Tests cover 200 and 503 responses
    - Tests verify component checks
  </verify>
  <done>
    RED: Failing tests created for enhanced health endpoint
  </done>
</task>

<task type="auto">
  <name>GREEN: Implement enhanced health endpoint</name>
  <files>src/api/routes/health.ts, src/events/event-bus.ts, src/events/sse-manager.ts</files>
  <action>
    Implement health endpoint enhancements to pass tests:

    1. Add health methods to event-bus.ts:
       - isActive(): boolean
       - getStats(): { listenerCount: number }

    2. Add health methods to sse-manager.ts:
       - isHealthy(): boolean
       - getStats(): { clientCount: number, uptime: number }

    3. Update health.ts:
       - Import eventBus and sseManager
       - Add component checks
       - Include stats in response
       - Import version from package.json

    Run tests and verify they pass.
    Commit: "feat(17-02): enhance health endpoint with component status"
  </action>
  <verify>
    - All health tests pass
    - EventBus and SSEManager have health methods
  </verify>
  <done>
    GREEN: Health endpoint implementation passes all tests
  </done>
</task>

<task type="auto">
  <name>REFACTOR: Clean up and optimize implementations</name>
  <files>src/config/env.ts, src/utils/exit-codes.ts, src/api/routes/health.ts</files>
  <action>
    Refactor implementations for clarity and maintainability:

    1. Review config validation:
       - Clear error messages
       - Good defaults
       - Type transforms work correctly

    2. Review exit codes:
       - All constants exported
       - Documentation complete
       - Type safe

    3. Review health endpoint:
       - Clean component checks
       - Proper error handling
       - Good test coverage

    4. Ensure no code duplication

    5. Add any missing documentation comments

    Run full test suite to ensure no regressions.
    Commit: "refactor(17-04): clean up reliability implementations"
  </action>
  <verify>
    - All tests still pass
    - Code is clean and documented
    - No regressions introduced
  </verify>
  <done>
    REFACTOR: Code cleaned up, all tests pass, ready for integration
  </done>
</task>

</tasks>

<verification>
  - All new tests pass
  - Existing tests still pass (no regressions)
  - Config validation fails fast with clear errors
  - Health endpoint returns component status
  - Exit codes match sysexits.h standard
</verification>

<success_criteria>
  1. Config validation has comprehensive test coverage
  2. Exit codes have test coverage
  3. Health endpoint has test coverage for component checks
  4. All tests pass (RED→GREEN→REFACTOR cycle complete)
  5. No regressions in existing tests
  6. Test files follow existing patterns
</success_criteria>

<output>
After completion, create `.planning/phases/17-core-reliability-fundamentals/17-04-SUMMARY.md`
</output>
