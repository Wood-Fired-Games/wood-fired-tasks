---
phase: 17-core-reliability-fundamentals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
  - src/api/server.ts
  - src/api/start.ts
autonomous: true
requirements:
  - RELI-01
  - RELI-06
must_haves:
  truths:
    - Service validates all required environment variables at startup
    - Service fails fast with clear error message on invalid/missing configuration
    - Service emits structured JSON logs in production mode
    - Sensitive fields are redacted in production logs
  artifacts:
    - path: src/config/env.ts
      provides: Zod schema for environment variable validation with clear error messages
      exports: [config, configSchema]
    - path: src/api/server.ts
      provides: Fastify server with Pino logging and redaction configured
      contains: logger.redact configuration
  key_links:
    - from: src/api/start.ts
      to: src/config/env.ts
      via: import { config } from '../config/env.js'
      pattern: config\.(PORT|HOST|LOG_LEVEL)
    - from: src/api/server.ts
      to: src/config/env.ts
      via: logger configuration using config values
      pattern: logger.*redact
---

<objective>
Implement fail-fast configuration validation with Zod and structured JSON logging with Pino redaction for sensitive fields.

Purpose: Ensure the service validates configuration at startup with clear error messages, and emits production-safe structured logs with sensitive data automatically redacted.
Output: src/config/env.ts with Zod schema, updated server.ts with Pino redaction, updated start.ts using validated config
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/wood-fired-bugs/src/api/start.ts
@/home/stuart/wood-fired-bugs/src/api/server.ts
</execution_context>

<context>
@/home/stuart/wood-fired-bugs/.planning/phases/17-core-reliability-fundamentals/17-RESEARCH.md
@/home/stuart/wood-fired-bugs/.planning/REQUIREMENTS.md
@/home/stuart/wood-fired-bugs/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration validation module with Zod</name>
  <files>src/config/env.ts</files>
  <action>
    Create src/config/env.ts with a Zod schema that validates and transforms environment variables:

    1. Define Config interface with typed fields
    2. Create configSchema with z.object() containing:
       - NODE_ENV: enum ['development', 'production', 'test'] with default 'development'
       - PORT: string transformed to number, default '3000'
       - HOST: string with default '0.0.0.0'
       - LOG_LEVEL: enum ['trace', 'debug', 'info', 'warn', 'error', 'fatal'] with default 'info'
       - API_KEYS: string with min(1) validation - REQUIRED (no default)
       - DATABASE_PATH: string with default './data/tasks.db'
       - CONNECTION_TIMEOUT: string transformed to number, default '120000' (2 min)
       - REQUEST_TIMEOUT: string transformed to number, default '60000' (1 min)
       - KEEP_ALIVE_TIMEOUT: string transformed to number, default '10000' (10 sec)

    3. Use safeParse() and on failure:
       - Log clear error messages to console.error showing each invalid field
       - Exit with sysexits.h EX_CONFIG (78) using process.exit(78)

    4. Export validated config object with proper types

    5. Add sysexits.h exit codes as constants:
       - EX_OK: 0
       - EX_USAGE: 64
       - EX_DATAERR: 65
       - EX_CONFIG: 78
       - EX_SOFTWARE: 70

    Follow pattern from 17-RESEARCH.md Pattern 2 (Configuration Validation with Zod).
    Avoid empty string issues by using .min(1) before transforms where needed.
  </action>
  <verify>
    - TypeScript compiles without errors
    - Test with invalid env vars: `API_KEYS= node -e "import('./dist/config/env.js')"` should exit 78 with clear error
    - Test with valid env vars: All fields properly typed and accessible
  </verify>
  <done>
    Configuration module validates env vars at startup, fails fast with exit code 78 on errors, exports typed config object with all required fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Fastify server with Pino structured logging and redaction</name>
  <files>src/api/server.ts</files>
  <action>
    Update src/api/server.ts to use the validated config and configure Pino logging with redaction:

    1. Import config from '../config/env.js'

    2. Update Fastify server creation to use config values:
       - connectionTimeout: config.CONNECTION_TIMEOUT
       - requestTimeout: config.REQUEST_TIMEOUT
       - keepAliveTimeout: config.KEEP_ALIVE_TIMEOUT
       - forceCloseConnections: 'idle' (requires Node.js >= 18.2.0)

    3. Configure logger with structured JSON output:
       - level: config.LOG_LEVEL
       - redact: only when NODE_ENV === 'production'
         paths: [
           'req.headers.authorization',
           'req.headers.cookie',
           'req.headers["x-api-key"]',
           '*.password',
           '*.secret',
           '*.apiKey',
           '*.token'
         ]
         censor: '[REDACTED]'
       - transport: only when NODE_ENV === 'development'
         target: 'pino-pretty'
         options: { colorize: true }

    4. Ensure the server registers the config so plugins can access it

    Follow pattern from 17-RESEARCH.md Pattern 1 (Structured Logging with Redaction).
    Use lowercase header names as Pino redaction is case-sensitive.
  </action>
  <verify>
    - TypeScript compiles without errors
    - Server starts with new configuration
    - Logs show JSON format in production, pretty format in development
  </verify>
  <done>
    Fastify server uses validated config, has Pino redaction configured for production, outputs structured JSON logs, uses forceCloseConnections: 'idle'
  </done>
</task>

<task type="auto">
  <name>Task 3: Update start.ts to use validated config and proper exit codes</name>
  <files>src/api/start.ts</files>
  <action>
    Update src/api/start.ts to use the new config module and sysexits.h exit codes:

    1. Import config at the TOP of the file (before createServer)
       import { config } from './config/env.js';
    This ensures validation runs immediately on module load.

    2. Replace hardcoded defaults with config values:
       - port: config.PORT (instead of parseInt(process.env.PORT || '3000'))
       - host: config.HOST (instead of process.env.HOST || '0.0.0.0')

    3. Update exit codes to use sysexits.h constants:
       - Successful shutdown: process.exit(0) (EX_OK)
       - Fatal errors during shutdown: process.exit(70) (EX_SOFTWARE)
       - Import config from env.ts which handles EX_CONFIG (78)

    4. Ensure shutdown handler uses correct exit codes:
       - Normal shutdown: exit code 0
       - Error during shutdown: exit code 70

    The config validation should run BEFORE createServer() is called.
  </action>
  <verify>
    - TypeScript compiles without errors
    - Server starts and uses config values
    - Missing API_KEYS env var causes immediate exit 78 with clear error message
    - Normal shutdown exits with code 0
  </verify>
  <done>
    start.ts imports config at top for fail-fast validation, uses config values for port/host, uses proper sysexits.h exit codes in shutdown handlers
  </done>
</task>

</tasks>

<verification>
  - Configuration validation runs before server creation
  - Invalid/missing required env vars cause exit 78 with clear error
  - Production logs show JSON with redacted sensitive fields
  - Development logs show pretty-printed colored output
  - Exit codes follow sysexits.h standard (0=success, 70=software error, 78=config error)
</verification>

<success_criteria>
  1. Service fails fast at startup with clear error if required env vars (API_KEYS) are missing
  2. Service exits with code 78 (EX_CONFIG) on configuration errors
  3. Production logs emit structured JSON with sensitive fields redacted as [REDACTED]
  4. Development logs use pino-pretty with colorized output
  5. All timeout values are configurable via environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/17-core-reliability-fundamentals/17-01-SUMMARY.md`
</output>
