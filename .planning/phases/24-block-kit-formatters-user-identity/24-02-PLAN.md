---
phase: 24-block-kit-formatters-user-identity
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/slack/formatters/project-formatter.ts
  - src/slack/formatters/__tests__/project-formatter.test.ts
autonomous: true
requirements:
  - BKIT-03

must_haves:
  truths:
    - "formatProjectList() returns KnownBlock[] with a header and one SectionBlock per project"
    - "formatProjectList() returns a 'no projects found' section for empty arrays"
    - "formatProjectDetail() returns a HeaderBlock with project name + SectionBlock with fields for ID, description, and timestamps"
  artifacts:
    - path: "src/slack/formatters/project-formatter.ts"
      provides: "formatProjectList, formatProjectDetail pure functions"
      exports: ["formatProjectList", "formatProjectDetail"]
    - path: "src/slack/formatters/__tests__/project-formatter.test.ts"
      provides: "Unit tests for project formatter functions"
      min_lines: 60
  key_links:
    - from: "src/slack/formatters/project-formatter.ts"
      to: "@slack/types"
      via: "type imports for KnownBlock, SectionBlock, HeaderBlock, DividerBlock"
      pattern: "import type.*from '@slack/types'"
    - from: "src/slack/formatters/project-formatter.ts"
      to: "src/types/task.ts"
      via: "domain type imports for Project"
      pattern: "import.*from '../../types/task"
---

<objective>
Create Block Kit formatter pure functions for project list and project detail responses.

Purpose: These formatters produce valid Block Kit JSON (`KnownBlock[]`) from `Project` domain objects, matching the formatting conventions established by the task formatters (Plan 01). Used by Phase 25 slash commands for `/tasks projects` and `/tasks project <id>`.

Output: `src/slack/formatters/project-formatter.ts` with `formatProjectList` and `formatProjectDetail` functions plus unit tests.
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-block-kit-formatters-user-identity/24-RESEARCH.md
@src/types/task.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for project formatters (RED)</name>
  <files>src/slack/formatters/__tests__/project-formatter.test.ts</files>
  <action>
Create the test file with comprehensive tests for `formatProjectList` and `formatProjectDetail`.

**Test structure for `formatProjectList`:**
- Returns `KnownBlock[]` with HeaderBlock "Projects (N)" and one SectionBlock per project
- Each project section shows `*#${id} ${name}*` with description preview (first 100 chars) or "_no description_"
- Empty array input returns single SectionBlock with "_No projects found._"
- DividerBlock separates projects in the list (between items, not after last)

**Test structure for `formatProjectDetail`:**
- Returns HeaderBlock with project name (plain_text, emoji: true, truncated to 150 chars if needed)
- Returns SectionBlock with fields array: ID, Description (or "_none_"), Created, Updated
- Description field truncates to 200 chars with "..." if longer

**Test data factory:** Create a `makeProject()` helper returning a complete `Project` with sensible defaults, allowing overrides.

Import functions from `../../project-formatter.js` (doesn't exist yet — tests MUST fail).

Run: `npx vitest run src/slack/formatters/__tests__/project-formatter.test.ts` — all tests MUST fail.
  </action>
  <verify>`npx vitest run src/slack/formatters/__tests__/project-formatter.test.ts 2>&1 | grep -E "FAIL|Cannot find module|does not provide"` shows failures</verify>
  <done>Test file exists with 8+ test cases covering both project formatters. All tests fail because the implementation module does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement project formatters to pass all tests (GREEN)</name>
  <files>src/slack/formatters/project-formatter.ts</files>
  <action>
Create `src/slack/formatters/project-formatter.ts` implementing both exported functions.

**`formatProjectList(projects: Project[]): KnownBlock[]`**
- Empty → `[{ type: 'section', text: { type: 'mrkdwn', text: '_No projects found._' } }]`
- Non-empty → HeaderBlock "Projects (N)" + for each project: SectionBlock with `*#${id} ${name}*\n${description preview or '_no description_'}` + DividerBlock between items (not after last)

**`formatProjectDetail(project: Project): KnownBlock[]`**
- HeaderBlock: project name (truncated to 150 chars, plain_text, emoji: true)
- SectionBlock with fields: `*ID*\n#${id}`, `*Description*\n${description || '_none_'}` (truncate to 200 chars), `*Created*\n${created_at}`, `*Updated*\n${updated_at}`

**Type imports:** Import `KnownBlock, SectionBlock, HeaderBlock, DividerBlock` from `@slack/types` as type-only. Import `Project` from `../../types/task.js`.

**Consistency with task formatters:** Use same patterns — HeaderBlock for titles, SectionBlock.fields for key/value pairs, mrkdwn text for content. This satisfies BKIT-03's "consistent structure matching task formatting conventions."

Run all tests — they MUST pass.
  </action>
  <verify>`npx vitest run src/slack/formatters/__tests__/project-formatter.test.ts` — all tests pass</verify>
  <done>All project formatter tests pass. formatProjectList and formatProjectDetail produce correct KnownBlock[] output matching task formatter conventions.</done>
</task>

</tasks>

<verification>
```bash
# All project formatter tests pass
npx vitest run src/slack/formatters/__tests__/project-formatter.test.ts

# Full test suite still passes
npx vitest run

# TypeScript compiles without errors
npx tsc --noEmit
```
</verification>

<success_criteria>
- formatProjectList returns valid KnownBlock[] with header and SectionBlocks per project
- formatProjectList handles empty input gracefully
- formatProjectDetail returns HeaderBlock + fields SectionBlock with ID, description, created/updated timestamps
- Project formatters use consistent Block Kit conventions matching task formatters (HeaderBlock, SectionBlock.fields, mrkdwn)
- All tests pass, full suite passes, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/24-block-kit-formatters-user-identity/24-02-SUMMARY.md`
</output>
