---
phase: 07-core-cli-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/cli/commands/create.ts
  - src/cli/commands/list.ts
  - src/cli/commands/update.ts
  - src/cli/output/formatters.ts
  - src/cli/__tests__/create.test.ts
  - src/cli/__tests__/list.test.ts
  - src/cli/__tests__/update.test.ts
autonomous: true

must_haves:
  truths:
    - "User runs 'tasks create --json' and receives parseable JSON envelope on stdout"
    - "User runs 'tasks list --json | jq' and jq parses successfully (no stderr contamination)"
    - "User sets NO_COLOR=1 and sees no ANSI codes in table output"
    - "User runs 'tasks create' without required flags and CLI prompts interactively (only if TTY)"
    - "User runs 'tasks update 123' and CLI shows colored table with improved status/priority formatting"
  artifacts:
    - path: "src/cli/commands/create.ts"
      provides: "JSON output support + interactive prompts"
      contains: "jsonOutput"
    - path: "src/cli/commands/list.ts"
      provides: "JSON output support for task list"
      contains: "jsonOutput"
    - path: "src/cli/commands/update.ts"
      provides: "JSON output support for updates"
      contains: "jsonOutput"
    - path: "src/cli/output/formatters.ts"
      provides: "NO_COLOR environment variable support"
      contains: "process.env.NO_COLOR"
  key_links:
    - from: "src/cli/commands/create.ts"
      to: "jsonOutput"
      via: "import and call in action handler"
      pattern: "jsonOutput\\(task"
    - from: "src/cli/commands/list.ts"
      to: "jsonOutput"
      via: "import and call in action handler"
      pattern: "jsonOutput\\(tasks"
    - from: "src/cli/output/formatters.ts"
      to: "chalk"
      via: "conditional import based on NO_COLOR check"
      pattern: "process\\.env\\.NO_COLOR"
---

<objective>
Retrofit existing CLI commands with JSON output support, interactive prompts, and NO_COLOR compliance.

Purpose: Complete the CLI infrastructure by integrating Wave 1 foundations (JSON output + prompts) into existing commands. Ensure all 3 current commands support --json flag, interactive prompting, and NO_COLOR standard.

Output:
- create/list/update commands support --json output
- create command prompts for missing required fields (when TTY)
- All commands respect NO_COLOR environment variable
- Updated tests covering new functionality
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-cli-infrastructure/07-01-PLAN.md
@.planning/phases/07-core-cli-infrastructure/07-02-PLAN.md
@src/cli/commands/create.ts
@src/cli/commands/list.ts
@src/cli/commands/update.ts
@src/cli/output/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Add NO_COLOR support to formatters</name>
  <files>src/cli/output/formatters.ts</files>
  <action>
    Update src/cli/output/formatters.ts:

    1. Add shouldUseColor() helper at top:
       - Returns false if process.env.NO_COLOR is set (any value)
       - Returns false if process.argv.includes('--json')
       - Returns true otherwise
       - Document: "Respects NO_COLOR env var per cli.dev standard"

    2. Update formatStatus() and formatPriority():
       - Wrap all chalk calls with shouldUseColor() check
       - Return plain strings when shouldUseColor() is false
       - Example: `shouldUseColor() ? chalk.green(status) : status`

    3. Update formatTaskTable():
       - Check shouldUseColor() before applying chalk to headers
       - Apply to table borders via style.border config conditionally

    4. Update formatTaskDetail():
       - Check shouldUseColor() before applying chalk.bold to labels

    Implementation notes:
    - NO_COLOR is a standard (https://no-color.org) - ANY value disables colors
    - Preserve existing color behavior when NO_COLOR not set
    - JSON mode already disables colors via isJsonMode() check (from Plan 01)
    - Combine checks: !shouldUseColor() || isJsonMode()
  </action>
  <verify>
    npx tsc --noEmit
    grep -q "process.env.NO_COLOR" src/cli/output/formatters.ts
    grep -q "shouldUseColor" src/cli/output/formatters.ts
    NO_COLOR=1 npx tsx src/cli/bin/tasks.ts list 2>&1 | grep -v "\x1b\[" || echo "No ANSI codes found - PASS"
  </verify>
  <done>
    - shouldUseColor() function checks NO_COLOR env var and --json flag
    - All chalk calls wrapped with shouldUseColor() check
    - formatters return plain strings when colors disabled
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Retrofit commands with JSON output and interactive prompts</name>
  <files>
    src/cli/commands/create.ts
    src/cli/commands/list.ts
    src/cli/commands/update.ts
  </files>
  <action>
    Update src/cli/commands/create.ts:
    1. Import jsonOutput from '../output/json-output.js'
    2. Import promptForMissing from '../prompts/interactive.js'
    3. In action handler, check if --json flag present: `program.optsWithGlobals().json`
    4. Make --title, --project, --created-by optional (remove requiredOption)
    5. Add interactive prompting BEFORE validation:
       - title = await promptForMissing('title', options.title)
       - project = await promptForMissing('project', options.project, {validate: isNumber})
       - createdBy = await promptForMissing('created-by', options.createdBy)
    6. After successful createTask(), check if JSON mode:
       - If --json: call jsonOutput({task}, {id: task.id})
       - Else: existing console.log with formatTaskDetail
    7. Update error handling to write errors to stderr in JSON mode

    Update src/cli/commands/list.ts:
    1. Import jsonOutput from '../output/json-output.js'
    2. After successful listTasks(), check if JSON mode
    3. If --json: call jsonOutput(tasks, {count: tasks.length})
    4. Else: existing formatTaskTable logic
    5. Move "No tasks found" message to stderr (messageOutput from json-output)

    Update src/cli/commands/update.ts:
    1. Import jsonOutput from '../output/json-output.js'
    2. After successful updateTask(), check if JSON mode
    3. If --json: call jsonOutput({task}, {id: task.id})
    4. Else: existing console.log with formatTaskDetail
    5. Move success messages to stderr

    Implementation notes from research:
    - Access global options via program.optsWithGlobals() (Commander.js pattern)
    - Interactive prompts only run when !--no-input AND process.stdin.isTTY (handled by promptForMissing)
    - JSON mode: ALL informational output goes to stderr, ONLY JSON envelope to stdout
    - Preserve existing validation logic (status/priority checks)
    - Do NOT prompt in JSON mode (--json implies --no-input behavior)
  </action>
  <verify>
    npx tsc --noEmit
    grep -q "jsonOutput" src/cli/commands/create.ts
    grep -q "jsonOutput" src/cli/commands/list.ts
    grep -q "jsonOutput" src/cli/commands/update.ts
    grep -q "promptForMissing" src/cli/commands/create.ts
    npx tsx src/cli/bin/tasks.ts create --help | grep -v "required" | grep "title" # Should be optional
  </verify>
  <done>
    - create.ts prompts for missing title/project/createdBy (when interactive)
    - All three commands support --json output with envelope format
    - JSON output goes to stdout, messages go to stderr
    - create command flags changed from requiredOption to option
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Update tests for new JSON and prompt functionality</name>
  <files>
    src/cli/__tests__/create.test.ts
    src/cli/__tests__/list.test.ts
    src/cli/__tests__/update.test.ts
  </files>
  <action>
    Update src/cli/__tests__/create.test.ts:
    1. Mock promptForMissing to return test values immediately (no actual prompting in tests)
    2. Add test: "outputs JSON when --json flag set" - verify stdout is valid JSON with envelope
    3. Update existing tests to provide all required fields via flags (since they're now optional)
    4. Add test: "prompts for missing title" - verify promptForMissing called when title not provided

    Update src/cli/__tests__/list.test.ts:
    1. Add test: "outputs JSON when --json flag set" - verify JSON envelope with tasks array and count metadata
    2. Add test: "JSON output parseable by jq" - verify JSON.parse doesn't throw
    3. Verify existing tests still pass with updated command behavior

    Update src/cli/__tests__/update.test.ts:
    1. Add test: "outputs JSON when --json flag set" - verify JSON envelope with updated task
    2. Verify existing tests still pass

    Testing notes:
    - Mock both json-output and prompts modules to control behavior
    - Use Vitest's importOriginal pattern (established in Phase 03-02)
    - Verify stdout vs stderr separation in JSON mode
    - No actual TTY interaction in tests (mocks handle all prompting)
  </action>
  <verify>
    npm test -- create.test
    npm test -- list.test
    npm test -- update.test
    npm test # All tests pass
  </verify>
  <done>
    - create.test.ts has tests for JSON output and interactive prompts
    - list.test.ts has tests for JSON output with count metadata
    - update.test.ts has tests for JSON output
    - All new tests pass
    - No test regressions (existing tests still pass)
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. All tests pass: `npm test` (should show 150+ tests passing)
3. NO_COLOR works: `NO_COLOR=1 npx tsx src/cli/bin/tasks.ts list` shows no ANSI codes
4. JSON output works: `npx tsx src/cli/bin/tasks.ts list --json` outputs valid JSON to stdout
5. Interactive prompts work: `npx tsx src/cli/bin/tasks.ts create` prompts for missing fields (in TTY)
6. Script mode works: `echo "" | npx tsx src/cli/bin/tasks.ts create --no-input` fails with clear error
</verification>

<success_criteria>
- All 3 commands (create, list, update) support --json flag
- JSON output is parseable (valid JSON envelope on stdout only)
- create command prompts for missing required fields when interactive
- All commands respect NO_COLOR environment variable
- Tests updated with coverage for JSON output and prompts
- No regressions: all existing tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-cli-infrastructure/07-03-SUMMARY.md`
</output>
