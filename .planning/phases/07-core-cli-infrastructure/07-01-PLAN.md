---
phase: 07-core-cli-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/bin/tasks.ts
  - src/cli/output/formatters.ts
  - src/cli/output/json-output.ts
autonomous: true

must_haves:
  truths:
    - "User runs 'tasks list --json' and receives only valid JSON on stdout (no messages)"
    - "User runs 'tasks list --json' and sees informational messages on stderr"
    - "User pipes 'tasks list --json | jq' and jq parses successfully"
    - "JSON output has consistent envelope: {success, data, metadata}"
    - "Global --json flag works on all CLI commands without per-command registration"
  artifacts:
    - path: "src/cli/output/json-output.ts"
      provides: "JSON envelope formatting and output abstraction"
      exports: ["jsonOutput", "JsonEnvelope", "OutputMode"]
    - path: "src/cli/bin/tasks.ts"
      provides: "Global --json flag registration"
      contains: "program.option('--json'"
    - path: "src/cli/output/formatters.ts"
      provides: "Updated formatters aware of output mode"
      min_lines: 120
  key_links:
    - from: "src/cli/bin/tasks.ts"
      to: "global --json option"
      via: "program.option before subcommand registration"
      pattern: "program\\.option\\('--json'"
    - from: "src/cli/commands/*.ts"
      to: "jsonOutput function"
      via: "import and call in action handlers"
      pattern: "import.*jsonOutput|jsonOutput\\("
---

<objective>
Create output abstraction layer with global --json flag for machine-readable output.

Purpose: Enable scripts and agents to consume CLI output reliably by separating data (stdout) from messages (stderr) and providing consistent JSON envelope format. This is the foundation for all 16 new CLI commands in Phase 8.

Output:
- JSON output utility with envelope format {success, data, metadata}
- Global --json flag registered in Commander.js program
- Output abstraction: data to stdout, messages to stderr
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli/bin/tasks.ts
@src/cli/commands/create.ts
@src/cli/commands/list.ts
@src/cli/output/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Create JSON output utility and update CLI entry point</name>
  <files>
    src/cli/output/json-output.ts
    src/cli/bin/tasks.ts
  </files>
  <action>
    Create src/cli/output/json-output.ts with:
    - JsonEnvelope type: {success: boolean, data: unknown, metadata?: {count?: number, timestamp?: string}}
    - jsonOutput(data: unknown, metadata?: object) function that writes JSON envelope to stdout
    - messageOutput(message: string) function that writes to stderr (respects process.stdout.isTTY check)
    - Output abstraction: console.log becomes stdout.write for data, stderr.write for messages

    Update src/cli/bin/tasks.ts:
    - Add global --json option BEFORE subcommand registration: program.option('--json', 'Output as JSON')
    - Use .parseAsync() instead of .parse() (Commander.js async handler requirement from research)
    - Document that commands access via opts() or optsWithGlobals()

    Follow patterns:
    - TypeScript strict mode (no any types)
    - Export types and functions from json-output.ts
    - Preserve existing Commander.js program structure
    - NO breaking changes to existing command signatures
  </action>
  <verify>
    npx tsc --noEmit (TypeScript compilation succeeds)
    grep -q "program.option('--json'" src/cli/bin/tasks.ts
    grep -q "parseAsync" src/cli/bin/tasks.ts
    grep -q "export.*jsonOutput" src/cli/output/json-output.ts
    grep -q "export.*JsonEnvelope" src/cli/output/json-output.ts
  </verify>
  <done>
    - json-output.ts exports jsonOutput function and JsonEnvelope type
    - Global --json flag registered in tasks.ts before subcommands
    - program uses parseAsync() for async command compatibility
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Update formatters to support output mode detection</name>
  <files>src/cli/output/formatters.ts</files>
  <action>
    Update src/cli/output/formatters.ts to add:
    - isJsonMode() helper that checks Commander.js global options for --json flag
    - Update formatStatus() and formatPriority() to return plain strings when in JSON mode (no chalk colors)
    - Add stripAnsiIfJsonMode(text: string): string utility that removes ANSI codes in JSON mode
    - Export isJsonMode for use in command handlers

    Implementation notes:
    - Access global options via program.optsWithGlobals() from Commander (research finding)
    - Do NOT import program directly (circular dependency risk) - pass mode as parameter OR check process.argv
    - Simpler approach: check process.argv.includes('--json') since this is output formatting only
    - Preserve existing color behavior when --json not set
    - Document that JSON mode disables all ANSI color codes

    Do NOT:
    - Break existing formatTaskTable or formatTaskDetail functions
    - Change function signatures (callers in create/list/update commands depend on current signatures)
  </action>
  <verify>
    npx tsc --noEmit
    grep -q "isJsonMode" src/cli/output/formatters.ts
    grep -q "process.argv.includes('--json')" src/cli/output/formatters.ts
    npm test -- formatters (if formatter tests exist)
  </verify>
  <done>
    - formatters.ts detects JSON mode from process.argv
    - formatStatus/formatPriority return plain strings in JSON mode
    - isJsonMode() exported for command handlers
    - Existing formatter functions unchanged (no signature changes)
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Test suite passes: `npm test`
3. Global option registered: `npx tsx src/cli/bin/tasks.ts --help` shows --json
4. JSON output utility exists with correct exports
5. Formatters detect JSON mode without breaking existing behavior
</verification>

<success_criteria>
- src/cli/output/json-output.ts exists with jsonOutput function and JsonEnvelope type
- Global --json flag visible in `tasks --help` output
- src/cli/bin/tasks.ts uses parseAsync() for async compatibility
- Formatters detect JSON mode and disable colors when --json present
- No TypeScript errors
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-cli-infrastructure/07-01-SUMMARY.md`
</output>
