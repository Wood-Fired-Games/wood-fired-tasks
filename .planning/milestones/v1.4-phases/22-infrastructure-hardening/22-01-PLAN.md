---
phase: 22-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/wood-fired-bugs.service
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "systemd service unit includes MemoryMax and CPUQuota resource limits"
    - "systemd security hardening options include ProtectKernelTunables, PrivateDevices, and SystemCallFilter"
    - "Service configuration is compatible with Node.js V8 JIT (MemoryDenyWriteExecute=no)"
    - "Service configuration preserves User=stuart for SQLite file ownership stability"
  artifacts:
    - path: "deploy/wood-fired-bugs.service"
      provides: "Hardened systemd service unit with resource limits and security directives"
      contains: "MemoryMax"
---

<objective>
Add resource limits and comprehensive security hardening to the systemd service unit.

Purpose: The existing service file has basic security (ProtectSystem, NoNewPrivileges). This plan adds resource limits to prevent runaway processes (INFR-01) and deeper security directives following systemd best practices (INFR-02).

Output: Updated `deploy/wood-fired-bugs.service` with resource limits and ~15 additional security directives.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add resource limits and security hardening to systemd service</name>
  <files>
    deploy/wood-fired-bugs.service
  </files>
  <action>
    Update `deploy/wood-fired-bugs.service` to add resource limits and comprehensive security hardening directives.
  </action>
  <verify>
    Read the updated service file and verify all directives are present. Verify MemoryDenyWriteExecute is NOT enabled (V8 JIT incompatible). Verify User=stuart is preserved (not DynamicUser).
  </verify>
  <done>
    Service file updated with MemoryMax=512M, CPUQuota=100%, TasksMax=50, and 15+ security hardening directives.
  </done>
</task>

</tasks>

<verification>
1. Service file contains MemoryMax=512M
2. Service file contains CPUQuota=100%
3. Service file contains TasksMax=50
4. Service file contains ProtectKernelTunables=yes
5. Service file contains PrivateDevices=yes
6. Service file contains SystemCallFilter=@system-service
7. MemoryDenyWriteExecute is NOT present or set to no
8. User=stuart is preserved (not DynamicUser)
</verification>

<success_criteria>
- systemd service unit includes MemoryMax and CPUQuota limits
- systemd security hardening options applied
- Configuration compatible with Node.js V8 JIT
- No breaking changes to existing service behavior
</success_criteria>

<output>
After completion, create `.planning/phases/22-infrastructure-hardening/22-01-SUMMARY.md`
</output>
