---
phase: 17-core-reliability-fundamentals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/routes/health.ts
  - src/api/server.ts
  - src/events/event-bus.ts
  - src/events/sse-manager.ts
autonomous: true
requirements:
  - RELI-02
  - RELI-04
must_haves:
  truths:
    - Health check endpoint returns component status
    - Database connectivity verified with SELECT 1 on each health check
    - Service returns 503 when database is unhealthy
    - Connection timeouts prevent hung requests
  artifacts:
    - path: src/api/routes/health.ts
      provides: Enhanced health endpoint with component status reporting
      exports: [default]
      min_lines: 80
    - path: src/api/server.ts
      provides: Server with timeout configurations
      contains: connectionTimeout, requestTimeout, keepAliveTimeout
  key_links:
    - from: src/api/routes/health.ts
      to: src/events/event-bus.ts
      via: eventBus.isActive() or similar health method
      pattern: eventBus\.(isActive|isHealthy)
    - from: src/api/routes/health.ts
      to: src/events/sse-manager.ts
      via: sseManager.isHealthy() or similar health method
      pattern: sseManager\.(isHealthy|getStats)
---

<objective>
Enhance health check endpoint with component status reporting and configure connection timeouts to prevent hung requests.

Purpose: Enable monitoring systems to verify service health including database connectivity and component status, while preventing resource exhaustion from hung connections.
Output: Enhanced health endpoint with detailed checks, server timeout configurations
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/wood-fired-bugs/src/api/routes/health.ts
</execution_context>

<context>
@/home/stuart/wood-fired-bugs/.planning/phases/17-core-reliability-fundamentals/17-RESEARCH.md
@/home/stuart/wood-fired-bugs/src/api/server.ts
@/home/stuart/wood-fired-bugs/src/events/event-bus.ts
@/home/stuart/wood-fired-bugs/src/events/sse-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health check methods to EventBus and SSEManager</name>
  <files>src/events/event-bus.ts, src/events/sse-manager.ts</files>
  <action>
    Add health check methods to EventBus and SSEManager for component status reporting:

    In src/events/event-bus.ts:
    1. Add isActive() method that returns true if emitter has listeners
    2. Add getStats() method returning { listenerCount: number }
    3. Keep methods simple - EventBus is always healthy if instantiated

    In src/events/sse-manager.ts:
    1. Add isHealthy() method returning true if SSE server is running
    2. Add getStats() method returning:
       - clientCount: number of active SSE connections
       - uptime: ms since creation
       - totalEventsSent: count of events sent
    3. Track clientCount and totalEventsSent internally

    These methods enable the health endpoint to report component status.
    Follow pattern from 17-RESEARCH.md Pattern 5 (Health Check with Component Status).
  </action>
  <verify>
    - TypeScript compiles without errors
    - EventBus.getStats() returns listener count
    - SSEManager.getStats() returns client count and uptime
  </verify>
  <done>
    EventBus has isActive() and getStats() methods; SSEManager has isHealthy() and getStats() methods returning connection stats
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance health endpoint with component status reporting</name>
  <files>src/api/routes/health.ts</files>
  <action>
    Enhance the health endpoint to report detailed component status:

    1. Import eventBus and sseManager instances

    2. Update the checks object to include:
       - database: 'ok' | 'failed'
       - eventBus: 'ok' | 'degraded' | 'unknown'
       - sseManager: 'ok' | 'degraded' | 'unknown'

    3. For database check (existing): fastify.db.prepare('SELECT 1').get()

    4. For eventBus check:
       - status = eventBus.isActive() ? 'ok' : 'degraded'

    5. For sseManager check:
       - status = sseManager.isHealthy() ? 'ok' : 'degraded'

    6. Add optional stats to response:
       - eventBus: { listenerCount: number }
       - sseManager: { clientCount: number, uptime: number }

    7. Response schema:
       - 200: healthy (all checks pass)
       - 503: unhealthy (database failed) or degraded (non-critical components)

    8. Keep database as the only critical check (503 if failed)
       Non-critical components (eventBus, sseManager) report degraded but don't fail health check

    9. Include version from package.json (use import or require)

    Follow pattern from 17-RESEARCH.md Pattern 5.
  </action>
  <verify>
    - TypeScript compiles without errors
    - GET /health returns 200 with component statuses when database is healthy
    - GET /health returns 503 when database check fails
    - Response includes timestamp, version, and detailed checks object
  </verify>
  <done>
    Health endpoint returns 200/503 status, includes database, eventBus, sseManager checks with detailed stats, follows schema validation
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify timeout configurations are properly applied</name>
  <files>src/api/server.ts</files>
  <action>
    Verify and document the timeout configurations added in Plan 01:

    1. Ensure server.ts has these Fastify options set:
       - connectionTimeout: from config (default 120000 = 2 min)
       - requestTimeout: from config (default 60000 = 1 min)
       - keepAliveTimeout: from config (default 10000 = 10 sec)

    2. Add comments explaining each timeout:
       - connectionTimeout: Socket inactivity timeout
       - requestTimeout: Maximum time for entire request to complete
       - keepAliveTimeout: Time to keep idle connections alive

    3. Verify forceCloseConnections: 'idle' is set for graceful shutdown

    4. Ensure all timeout values are in milliseconds

    These configurations prevent hung requests and resource exhaustion.
    Follow recommendations from 17-RESEARCH.md Configuration section.
  </action>
  <verify>
    - All three timeout options are configured with sensible defaults
    - Values are documented with comments
    - forceCloseConnections: 'idle' is set
  </verify>
  <done>
    Server has connectionTimeout, requestTimeout, keepAliveTimeout configured with documented values from config
  </done>
</task>

</tasks>

<verification>
  - GET /health returns 200 when database is healthy
  - GET /health returns 503 when database is down
  - Health response includes component status for database, eventBus, sseManager
  - Health response includes version from package.json
  - Timeouts prevent hung connections (test with slow requests)
</verification>

<success_criteria>
  1. Health endpoint at GET /health verifies DB connectivity with SELECT 1
  2. Health endpoint reports component status for database, eventBus, and sseManager
  3. Returns 503 when database check fails
  4. Connection timeouts (connectionTimeout, requestTimeout, keepAliveTimeout) are configured
  5. Health response includes timestamp, version, and checks object
</success_criteria>

<output>
After completion, create `.planning/phases/17-core-reliability-fundamentals/17-02-SUMMARY.md`
</output>
