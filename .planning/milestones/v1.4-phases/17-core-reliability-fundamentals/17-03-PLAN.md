---
phase: 17-core-reliability-fundamentals
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/api/start.ts
  - src/api/server.ts
  - src/utils/exit-codes.ts
autonomous: true
requirements:
  - RELI-03
  - RELI-07
  - RELI-08
must_haves:
  truths:
    - Service gracefully shuts down on SIGTERM/SIGINT
    - Idle connections are closed before shutdown completes
    - WAL checkpoint runs during shutdown to reclaim disk space
    - Exit codes follow sysexits.h standard
  artifacts:
    - path: src/utils/exit-codes.ts
      provides: sysexits.h constants for standard exit codes
      exports: [ExitCodes]
    - path: src/api/start.ts
      provides: Graceful shutdown handler with WAL checkpoint
      contains: shutdown function with db.pragma('wal_checkpoint')
  key_links:
    - from: src/api/start.ts
      to: src/utils/exit-codes.ts
      via: import { ExitCodes }
      pattern: ExitCodes\.(EX_OK|EX_SOFTWARE)
    - from: src/api/start.ts
      to: src/api/server.ts
      via: server.close() with forceCloseConnections: 'idle'
      pattern: await server\.close\(\)
---

<objective>
Implement graceful shutdown with idle connection closing and WAL checkpointing, plus periodic WAL checkpoints to prevent file bloat.

Purpose: Ensure clean shutdown without data loss, prevent unbounded WAL file growth, and use standard exit codes for script integration.
Output: Enhanced shutdown handler, periodic WAL checkpoint scheduling, exit codes module
</objective>

<execution_context>
@/home/stuart/.claude/get-shit-done/workflows/execute-plan.md
@/home/stuart/wood-fired-bugs/src/api/start.ts
</execution_context>

<context>
@/home/stuart/wood-fired-bugs/.planning/phases/17-core-reliability-fundamentals/17-RESEARCH.md
@/home/stuart/wood-fired-bugs/src/db/database.ts
@/home/stuart/wood-fired-bugs/src/events/sse-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exit codes utility module</name>
  <files>src/utils/exit-codes.ts</files>
  <action>
    Create src/utils/exit-codes.ts with sysexits.h standard exit codes:

    1. Define ExitCodes object with constants:
       - EX_OK: 0 - Success
       - EX_USAGE: 64 - Command line usage error
       - EX_DATAERR: 65 - Data format error
       - EX_NOINPUT: 66 - Cannot open input
       - EX_UNAVAILABLE: 69 - Service unavailable
       - EX_SOFTWARE: 70 - Internal software error
       - EX_OSERR: 71 - System error
       - EX_CANTCREAT: 73 - Cannot create output file
       - EX_IOERR: 74 - I/O error
       - EX_TEMPFAIL: 75 - Temporary failure
       - EX_PROTOCOL: 76 - Remote error in protocol
       - EX_NOPERM: 77 - Permission denied
       - EX_CONFIG: 78 - Configuration error

    2. Export as const assertion for type safety

    3. Add JSDoc comments for each code explaining usage

    4. Also export a simplified CliExitCodes for CLI use:
       - SUCCESS: 0
       - GENERAL_ERROR: 1
       - USAGE_ERROR: 2
       - CONFIG_ERROR: 78

    Follow pattern from 17-RESEARCH.md Exit Codes Module section.
    This provides self-documenting exit codes for all scripts.
  </action>
  <verify>
    - TypeScript compiles without errors
    - Exit codes match sysexits.h standard values
    - Both ExitCodes and CliExitCodes are exported
  </verify>
  <done>
    Exit codes module exports all sysexits.h constants with documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance graceful shutdown with WAL checkpoint</name>
  <files>src/api/start.ts</files>
  <action>
    Enhance the shutdown handler in start.ts to perform WAL checkpoint:

    1. Import ExitCodes from '../utils/exit-codes.js'

    2. Update shutdown function to:
       a. Log shutdown start with signal name
       b. Stop SSE manager (close all SSE connections gracefully)
          - sseManager.stop() or similar method
       c. Close server with forceCloseConnections: 'idle' (already configured in server.ts)
          - await server.close()
       d. Run WAL checkpoint with TRUNCATE mode:
          - app.db.pragma('wal_checkpoint(TRUNCATE)')
          - Log checkpoint completion
       e. Close database connection:
          - app.db.close()
       f. Log shutdown complete
       g. Exit with ExitCodes.EX_OK (0)

    3. On error during shutdown:
       - Log fatal error
       - Exit with ExitCodes.EX_SOFTWARE (70)

    4. Ensure shutdown is async and handles errors properly

    5. Add timeout for graceful shutdown (optional but recommended):
       - If shutdown takes >30 seconds, force exit with EX_SOFTWARE

    Follow pattern from 17-RESEARCH.md Pattern 3 (Graceful Shutdown with WAL Checkpoint).
    The forceCloseConnections: 'idle' option was set in server.ts in Plan 01.
  </action>
  <verify>
    - TypeScript compiles without errors
    - SIGTERM triggers graceful shutdown
    - WAL checkpoint runs during shutdown
    - Database connection closes cleanly
    - Exit code is 0 on success, 70 on error
  </verify>
  <done>
    Shutdown handler closes SSE connections, runs WAL checkpoint, closes database, exits with standard exit codes
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement periodic WAL checkpoint to prevent file bloat</name>
  <files>src/api/start.ts</files>
  <action>
    Add periodic WAL checkpoint scheduling to prevent unbounded WAL file growth:

    1. After server starts, set up a periodic checkpoint:
       - Interval: 15 minutes (15 * 60 * 1000 ms)
       - Configurable via env var: WAL_CHECKPOINT_INTERVAL_MS (default 900000)

    2. In the checkpoint callback:
       a. Log debug: 'Running periodic WAL checkpoint'
       b. Run: app.db.pragma('wal_checkpoint(TRUNCATE)')
       c. Log debug with result: { checkpointResult: result }
       d. Catch errors and log error: 'WAL checkpoint failed'

    3. Add cleanup on server close:
       - Use server.addHook('onClose', async () => { clearInterval(checkpointInterval); })

    4. Make checkpoint interval configurable via config:
       - Add to configSchema: WAL_CHECKPOINT_INTERVAL_MS with default 15 minutes

    5. Consider making checkpoint mode configurable:
       - PASSIVE: doesn't block writers but may not truncate
       - TRUNCATE: blocks writers but reclaims space (default)

    Follow pattern from 17-RESEARCH.md Pattern 4 (Periodic WAL Checkpoint).
    Use TRUNCATE mode for space reclamation.
  </action>
  <verify>
    - TypeScript compiles without errors
    - Periodic checkpoint runs every 15 minutes
    - Checkpoint interval is configurable
    - Interval cleared on server close
    - WAL file size stays bounded over time
  </verify>
  <done>
    Periodic WAL checkpoint runs every 15 minutes, configurable via env var, cleans up on server close
  </done>
</task>

</tasks>

<verification>
  - SIGTERM triggers graceful shutdown sequence
  - Server closes idle connections before shutdown
  - WAL checkpoint runs during shutdown
  - Database connection closes cleanly
  - Periodic checkpoint runs every 15 minutes
  - Exit codes follow sysexits.h: 0=success, 70=software error, 78=config error
</verification>

<success_criteria>
  1. Service gracefully shuts down on SIGTERM/SIGINT
  2. Idle connections are closed with forceCloseConnections: 'idle'
  3. WAL checkpoint runs during shutdown (pragma wal_checkpoint(TRUNCATE))
  4. Periodic WAL checkpoint runs every 15 minutes
  5. Exit codes follow sysexits.h standard (0=success, 70=software error)
  6. Shutdown completes within reasonable time (add timeout if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/17-core-reliability-fundamentals/17-03-SUMMARY.md`
</output>
