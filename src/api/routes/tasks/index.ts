import { FastifyPluginAsyncZod } from 'fastify-type-provider-zod';

const taskRoutes: FastifyPluginAsyncZod = async (fastify) => {
  // Placeholder - routes will be implemented in Task 2
};

export default taskRoutes;
